package lunaris.recipes.eval

import lunaris.recipes.Recipe
import org.broadinstitute.yootilz.core.snag.Snag

object LunCompiler {
  def compile(recipe: Recipe): Either[Snag, LunRunnable] = {
    ???
  }
}
